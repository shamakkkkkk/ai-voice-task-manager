services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_URL: file:/data/tasks.db   # SQLite file lives in the named volume below
    volumes:
      - task-data:/data

volumes:
  task-data:
